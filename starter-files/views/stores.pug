extends layout

include mixins/_storeCard
include mixins/_paginationBar

block content
	.inner
		h2= title
		.stores
			each store in stores
				+storeCard(store)
		+paginationBar(page, pages, count)
